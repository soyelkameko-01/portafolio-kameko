
import React, { useState } from 'react';
import Navbar from './components/Navbar';
import GalleryView from './components/GalleryView';
import ProjectsView from './components/ProjectsView';
import BookingView from './components/BookingView';
import { Section } from './types';

const App: React.FC = () => {
  const [active, setActive] = useState<Section>('galeria');

  return (
    <div className="min-h-screen bg-[#050505]">
      <Navbar current={active} set={setActive} />
      
      <main className="animate-in fade-in duration-1000">
        {active === 'galeria' && <GalleryView />}
        {active === 'proyectos' && <ProjectsView />}
        {active === 'reserva' && <BookingView />}
      </main>

      <footer className="py-20 text-center border-t border-white/5">
        <p className="text-[8px] font-bold text-neutral-700 uppercase tracking-[1em]">
          © 2024 EL KAMEKO • FOTOGRAFÍA COSPLAY
        </p>
      </footer>
    </div>
  );
};

export default App; 
